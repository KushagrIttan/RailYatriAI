# Python backend — optimization engine (FastAPI)
# The .NET API (RailBlockAI.Api) is not installed via pip.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
httpx>=0.27.0        # async HTTP client — used to pull data from .NET /api/optimization/data
